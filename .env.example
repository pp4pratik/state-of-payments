# Optional - pageview/session analytics via PostHog. Leave blank to disable.
VITE_POSTHOG_KEY=

# All data is static JSON under public/data/ and public/statewise-historical/ - no
# backend env vars needed. scripts/fetch_npci_data.py and scripts/fetch_rbi_data.py
# scrape their sources directly and write straight to those files.
